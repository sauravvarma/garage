.DS_Store
*.swp
*.swo
.idea/
.vscode/
node_modules/
*.log

# Local Claude Code state
.claude/

# Skill-creator eval artifacts (per-iteration runs, fixtures, benchmarks)
skills-workspace/

# Durable eval suite (evals/) IS committed — it travels with the skills it guards.
# Only per-run benchmark outputs under any runs/ dir stay local.
evals/**/runs/
